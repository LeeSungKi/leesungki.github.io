<h1 align="center">
  Re:teev Blog 
</h1>

**Demo Websites**: [**Korean**](https://leesungki.github.io/)

# 👋 소개

## 팀 블로그를 직접 운영하기위해 🙌



# 👌 확인하기
## 💻 Windows에서 Node.js 버전 관리 도구 `mvm` (nvm-windows) 사용법
> 이 문서는 Windows 환경에서 Node.js 버전을 쉽게 관리할 수 있도록 돕는 `nvm-windows` 도구 사용법을 설명합니다.
---
### 📌 1. mvm 또는 nvm-windows란?

Node.js는 다양한 버전이 존재하며, 프로젝트마다 요구하는 버전이 다를 수 있습니다.  
이를 효율적으로 관리하기 위해 **Node Version Manager (NVM)** 을 사용합니다.

Windows에서는 `nvm-windows`를 통해 동일한 기능을 사용할 수 있습니다.
> 참고: `mvm`은 macOS/Linux 전용이며, Windows에선 `nvm-windows`를 사용합니다.

---

## 🛠️ 2. 설치 방법

### ✅ Step 1: 설치 파일 다운로드

- GitHub Releases 페이지에서 다운로드  
  [https://github.com/coreybutler/nvm-windows/releases](https://github.com/coreybutler/nvm-windows/releases)

- `nvm-setup.exe` 파일을 선택하여 다운로드

### ✅ Step 2: 설치 진행

1. 다운로드한 `nvm-setup.exe` 실행
2. 설치 경로 설정 (기본값 사용 가능)
3. Node.js 설치 경로도 기본값으로 설정
4. 설치 완료 후 명령 프롬프트(CMD) 재시작

---

## 🧪 3. 주요 명령어

| 명령어 | 설명 |
|--------|------|
| `nvm list` 또는 `nvm ls` | 설치된 Node.js 버전 목록 확인 |
| `nvm install <버전>` | 해당 버전 설치 (예: `nvm install 18.17.0`) |
| `nvm use <버전>` | 특정 버전 사용 설정 |
| `nvm uninstall <버전>` | 버전 삭제 |
| `nvm current` | 현재 사용 중인 Node.js 버전 확인 |

---

## 🎯 4. 사용 예시

```bash
  nvm install 16.20.2      # Node.js 16.20.2 설치
  nvm list                 # 설치된 버전 목록 확인
  nvm use 16.20.2          # 18.17.0 버전으로 전환
  node -v                  # 현재 Node.js 버전 확인
```

# 🏃‍♀️ 실행하기
아래 명령어를 실행하여 로컬 환경에 블로그를 실행합니다.
```bash
  # Install dependencies
  $ npm install

  # Start development server
  $ npm start
```

위 명령어가 문제 없이 실행됐다면 [http://localhost:8000](http://localhost:8000)에서 블로그를 확인

# ❓ 배포하기
```bash
$ npm run deploy

Published라는 메시지를 받으셨다면 배포는 끝
```
